services:
  - type: web
    runtime: python
    name: contel-dashboard
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: DATABASE_URL
        sync: false
